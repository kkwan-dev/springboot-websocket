spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:example
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create
